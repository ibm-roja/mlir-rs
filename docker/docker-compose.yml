services:
  mlir-rs-test:
    image: mlir-rs-test:latest
    build: .
    volumes:
      - ..:/mlir-rs
    command: bash /mlir-rs/docker/scripts/test-all.sh
    cap_add:
      - ALL
    security_opt:
      - seccomp:unconfined
    environment:
      - CARGO_TARGET_DIR=~/target
